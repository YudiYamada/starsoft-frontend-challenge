export const theme = {
  colors: {
    primary: "#FF8310",     // Laranja principal
    background: "#191A20",  // O tom mais escuro da paleta
    surface: "#232323",     // Cinza escuro (cards/inputs)
    gray: "#393939",        // Cinza intermediário
    lightGray: "#CCCCCC",   // Cinza claro (textos secundários)
    white: "#FFFFFF",
  },
  borderRadius: "8px",
};

export type ThemeType = typeof theme;
